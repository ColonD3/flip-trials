# Flip Trials

A web-based random trick game with four tiers, fail-meter progression, and a neon UI.

## Run locally

Open `index.html` in a browser.

## Deploy to Netlify

Use `deploy_netlify.py` with a local Netlify token:

```bash
python3 deploy_netlify.py /path/to/project
```
